module.exports = {
    name: 'shuffle',
    run: async (client, interaction, parms, { MusicDB }) => {
        return interaction.reply(`Work in Progress: Will update on **Monday, 17th June 2024**`);
    }
}